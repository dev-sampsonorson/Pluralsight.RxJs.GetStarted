// import { helpers } from 'rxjs/'

/* let source$ = helpers.cold('-a-b-c');

let source$ = helpers.cold('--a-4---c-8|');

let source$ = helpers.cold('  --a-4 12ms c-8#'); // # represents error

// ^ represents the point where the subscription to a hot observable begins
let source$ = helpers.hot('-a-^-b-(cde)---f|'); */